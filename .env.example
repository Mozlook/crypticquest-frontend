# Copy to .env (or set in the Netlify panel for production).
# Base URL of the CrypticQuest backend API, no trailing slash.
#   local:      http://localhost:8080
#   production: https://api.your-domain.example
VITE_API_URL=http://localhost:8080
